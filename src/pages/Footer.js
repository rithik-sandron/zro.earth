import React, { memo, useState } from 'react';
import '../App.css';

export default memo(function Footer() {

    return (
        <div className='footer'>
            <span>
                sample content used for display purpose.
            </span>
        </div>
    )
});